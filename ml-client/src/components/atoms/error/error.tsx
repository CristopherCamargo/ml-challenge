import React from "react";
import { ErrorWrapper } from "./styles";

const Error = () => {
  return (
    <ErrorWrapper>
      Surgio un error, no se pudo conectar satisfactoriamente al servicio
      proporcionado.
    </ErrorWrapper>
  );
};

export default Error;
